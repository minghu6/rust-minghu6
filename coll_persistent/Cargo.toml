[package]
name = "m6-coll-persistent"
version = "0.1.3"
edition = "2021"

[dependencies]
common = { package = "m6-common", path = "../common" }
coll = { package = "m6-coll", path = "../coll" }

[package.metadata.workspaces]
independent = true
