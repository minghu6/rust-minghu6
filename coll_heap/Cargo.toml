[package]
name = "m6-coll-heap"
version = "0.1.7"
edition = "2021"

[dependencies]
common = { package = "m6-common", path = "../common" }
coll = { package = "m6-coll", path = "../coll" }

[dev-dependencies]
lazy_static = "1.4.0"

[package.metadata.workspaces]
independent = true
